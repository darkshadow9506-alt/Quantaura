# ---- Core numerical / data stack ----
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3          # gradient boosting (ML triple-barrier strategy)

# ---- Optional accelerator (the bot runs without it) ----
# statsmodels gives MacKinnon cointegration p-values for pairs trading; if it
# is missing, pairs falls back to a built-in numpy Engle-Granger test, so a
# failed/partial install no longer crashes the bot or selftest.
statsmodels>=0.14

# ---- Market data ----
yfinance>=0.2.40           # stocks, ETFs, FX, indices (free, EOD + intraday)
ccxt>=4.3                  # crypto exchange OHLCV (Binance, etc.)
requests>=2.28            # tgju.org (Iranian free-market gold & USD)

# ---- Telegram bot ----
python-telegram-bot[job-queue,socks]>=21.0  # bot framework + scheduler + SOCKS proxy
PySocks>=1.7              # SOCKS proxy support for requests-based data fetches

# ---- Config / utilities ----
PyYAML>=6.0
python-dotenv>=1.0
